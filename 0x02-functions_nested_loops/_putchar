#include <unistd.h>

/**
* _putchar - write the c code to stdout
* @c: the character to print 
*
* Return: on success 1.
* on erro, -1 is returned
*/
int _putchar(char c)
{
return (write(1, &c, 1));
}
