#include "main.h"
#include <stdio.h>
/**
*_abs - is a function that makes everithing positive
*@c: is the int used argument in the function
*Return: 0
*/
int _abs(int c)
{
if (c >= 0)
{
return (c)
}
else
{
return (c * -1)
}
}
