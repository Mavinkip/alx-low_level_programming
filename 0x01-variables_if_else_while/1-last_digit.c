#include <stdio.h>

#include <stdio.h>

#include <time.h>



/**
 *
 * * main - Entry point
 *
 * * 
 *
 * * Return: Always 0 (success)
 *
 * */int main(void)

{

	  int n;

	    int L;

	      char str[] = "last digit of";

	        

	       srand(time(0));

	        n = rand() - RAND_MAX / 2;

		 L = n % 10;



		   if (L > 5)

			       printf("%s %d is %d and is greater than 5\n", str, n, l);

		     else if (l > 5)

			         printf(%s %d is %d and is 0\n", str, n, l);

		       else if (l < 6)

			           printf(%s %d is %d and is less than 6 and not o\n", str, n, l);



		           return (0);

}


